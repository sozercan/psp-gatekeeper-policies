apiVersion: v1
kind: Pod
metadata:
  name: nginx-allowed-capabilities
  labels:
    app: nginx-allowed-capabilities
spec:
  containers:
  - name: nginx
    image: nginx
    securityContext:
      capabilities:
        add:
          - SYS_TIME